# ahp-cli

Tool for creating, running, and analyzing Analytic Hierachy Process models.

See `college.yaml` for an example model structure. 

Open a file with `ahp-cli file.yaml`, or just run `ahp-cli` and create the model in the explorer

Happy decision making!
